# Frontend (Next.js) for MCP SaaS

## Setup (development)
1. copy `.env.example` to `.env.local` and set `NEXT_PUBLIC_BACKEND_URL` to your backend URL (e.g. http://localhost:4000)
2. Install dependencies:
